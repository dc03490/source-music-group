# dependencies
node_modules/
.pnpm-store/

# builds
.next/
out/
dist/
build/
.turbo/

# SST: the platform and provider binaries are downloaded by `sst install`
# (~474MB). Never committed. Run `pnpm sst install` after cloning to get the
# component types that sst.config.ts and infra/ typecheck against.
.sst/

# env
.env
.env.*
!.env.example

# misc
.claude/
.DS_Store
*.log
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts
.vercel-links/
